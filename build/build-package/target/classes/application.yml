spring:
  profiles:
    active: activatedProperties@

server:
  banner:
    location: classpath:banner.txt

logging:
  level:
    org:
      chenile:
        security: DEBUG

cors:
  allowedOrigin: http://localhost:3000